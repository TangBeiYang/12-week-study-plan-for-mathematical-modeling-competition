#### 一、拆解题目

##### 1\.标关键词

关键词类型                                        例子                                    暗示模型

排名、评价、综合得分         "评价哪个城市更优"               评价类（TOPSIS、AHP）

预测、未来、趋势                "预测未来五年的销量"            预测类（回归、时间序列）

最优、最小、最大、约束     "在预算限制下最大化收益"     优化类（线性规划）

分类、聚类、相似                "将样本分成若干类"                聚类/分类

路径、网络、连接                "设计最优运输方案"                图模型

随时间变化、传播、演化     "描述疾病传播过程"                微分方程/动态模型

##### 2\.数清楚有几个问，每问的任务是什么

把每小问做的事用一句话概括

例：

第一问：描述数据基本特征

第二问：建立评价模型，对各方案排序

第三问：在给定约束下，找最优配置

第四问：分析模型对参数的敏感性

##### 3\.确认输入输出

输入：题目给了什么数据、条件?

输出：这问要我给出什么，排名、数值、路径?



#### 二、多模型组合

1\.数据处理->评价模型

清洗完的数据作为评价模型的指标矩阵输入

2\.评价/预测->优化

评价得分作为优化模型里的收益系数

例：给若干项目打分，每个项目有成本，总预算有限，问怎么选项目使总分最高

3\.微分方程->优化

微分方程的模拟结果作为优化目标函数的基础

例：先用 SIR 建立传播模型，再问"怎么分配防控资源使感染峰值最低"

4\.聚类->分类建模

聚类标签作为后续分组建模的依据

例：把全国城市按经济水平聚类，再对每类城市分别建预测模型，效果比统一建模好



#### 三、灵敏度分析

##### 1\.什么是灵敏度分析

问题：如果某个参数变化，结果会有多大变化

这个问题就是灵敏度分析，作用是：

说明模型的稳健性（结果不会因为参数微小变化就完全不同）

找出关键参数（哪些参数对结果影响最大）

让论文的结论更可信

##### 2\.怎么做

最简单的做法：单参数扰动法——固定其他参数，只改变一个参数的值（比如 ±10%、±20%），观察结果怎么变化，画出折线图

##### 3\.论文里该怎么写

例：

为验证模型的稳健性，本文对参数 β 在基准值 ±20% 范围内进行扰动，结果如图 X 所示。当 β 变化 10% 时，感染峰值变化约 X%，说明模型对该参数较为敏感 / 不敏感



#### 四、模型假设与局限

段落模板：



本文建立的 \[模型名] 具有以下优点：

1\. ...

2\. ...

同时，该模型也存在以下局限：

1\. 假设 \[某条件] 在现实中可能不完全成立，导致 \[某影响]。

2\. 模型未考虑 \[某因素]，在 \[某场景] 下可能引起误差。

后续改进方向：可以通过 \[某方法] 进一步完善。



#### 五、模型与解决问题汇总表

&#x20;         模型类型                     核心问题                                    典型输出                                      前置条件

评价（TOPSIS/熵权）  多对象多指标，谁更好                排名、综合得分                     有指标数据，需确定权重

AHP                              多对象多指标，主观确定权重     权重向量、排名                     需要专家判断或主观打分

线性回归                       变量间的线性关系、预测             预测值、系数                        因变量和自变量有线性关系

时间序列                       某量随时间的变化规律、预测      未来值                                   有时间序列历史数据

灰色预测 GM(1,1)        数据少、规律不明显时的预测      未来值                                   样本量小（4\~10个）

线性规划                       线性约束下最优化                        最优决策变量                        目标和约束均为线性

整数规划                       决策变量必须取整数                     整数最优解                           变量不可分（选或不选）

K-means                      聚类无标签数据分组                     类别标签、簇中心                 无先验分类标准

图模型（最短路）        点到点最小代价路径                     路径、代价值                        有节点-边-权值结构

图模型（最小生成树） 连通所有点最小总代价                 树结构、总代价                     需要连通所有节点

微分方程                       变量随时间的动态演化                 时间序列曲线                        能描述变化率与状态的关系

SIR 类模型                    传播、扩散、演化过程                 S/I/R 曲线、峰值时间系统    有明确的状态转移机制

